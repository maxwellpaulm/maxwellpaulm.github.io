[package]
name = "site"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
maud = "0.27"
serde = { version = "1.0", features = ["derive"] }
toml = "1.1"
anyhow = "1.0"
